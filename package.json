{
  "name": "protocol-418",
  "version": "418.11.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check-build.mjs",
    "test": "npm run build && npm run check"
  }
}
